import { PageHeader } from "@/components/ui/page-header";

export default function UserManagementPage() {
  return (
    <div>
      <PageHeader
        title="User Management"
        description="Manage admin and branch user accounts across the system."
      />
    </div>
  );
}
